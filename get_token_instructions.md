# 🔐 Инструкция по получению токена для создания уроков

## Шаг 1: Войдите в систему

Откройте браузер и перейдите по адресу:
```
http://192.168.110.178:5128/login
```

Используйте учётные данные:
- **Email**: `admin@test.com`
- **Password**: `admin123`

## Шаг 2: Откройте консоль разработчика

После успешного входа:
1. Нажмите `F12` (или `Cmd+Option+I` на Mac)
2. Перейдите на вкладку **Console**

## Шаг 3: Получите токен

В консоли выполните команду:
```javascript
localStorage.getItem('token')
```

Скопируйте токен (длинная строка, начинающаяся с `eyJ...`)

## Шаг 4: Сохраните токен

Выполните в терминале:
```bash
cd /Users/brandbox/Desktop/numerom/Numerom1
echo "ВАШ_ТОКЕН_ЗДЕСЬ" > .admin_token
```

Замените `ВАШ_ТОКЕН_ЗДЕСЬ` на скопированный токен.

## Шаг 5: Запустите скрипт

Теперь можно создавать уроки:
```bash
python3 create_lesson_from_folder.py 1
```

---

## Альтернативный способ (если токен не работает)

Если у вас уже есть доступ к админ-панели, вы можете создать урок вручную через интерфейс:

1. Перейдите: `http://192.168.110.178:5128/admin`
2. Откройте раздел **Уроки**
3. Нажмите **Создать урок**
4. Заполните все поля вручную

---

## Учётные данные

Созданный супер-админ:
- Email: `admin@test.com`
- Password: `admin123`
- Права: super_admin

---

## Проверка

Чтобы проверить, что токен сохранён правильно:
```bash
cat .admin_token
```

Должна отобразиться длинная строка токена.

