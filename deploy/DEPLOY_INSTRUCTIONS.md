# 🚀 Инструкция по развертыванию NumerOM

## 📋 Системные требования

- **Docker** версии 20.10+
- **Docker Compose** версии 1.29+
- **Оперативная память**: минимум 4GB
- **Дисковое пространство**: минимум 10GB

## 🛠️ Быстрый запуск

### 1. Распакуйте архив
```bash
unzip numerom_deploy.zip
cd numerom_deploy
```

### 2. Настройте переменные окружения
```bash
# Скопируйте файл с переменными окружения
cp backend/.env.example backend/.env

# Отредактируйте переменные в backend/.env
nano backend/.env
```

**Обязательные переменные:**
- `JWT_SECRET_KEY` - секретный ключ для JWT токенов
- `MONGODB_DATABASE` - имя базы данных (по умолчанию: numerom)

### 3. Запустите приложение
```bash
# Для production
docker-compose -f docker-compose.prod.yml up -d

# Или для разработки
docker-compose up -d
```

### 4. Проверьте работу
```bash
# Backend API
curl http://localhost:8001/docs

# Frontend
curl http://localhost:5128
```

## 📁 Структура проекта

```
numerom_deploy/
├── backend/                 # Python FastAPI сервер
│   ├── Dockerfile          # Docker образ backend
│   ├── requirements.txt    # Python зависимости
│   ├── server.py          # Основной сервер
│   └── ...                # Остальной код
├── frontend/               # React приложение
│   ├── build/             # Production build
│   ├── Dockerfile         # Docker образ frontend
│   └── nginx.conf         # Nginx конфигурация
├── docker-compose.yml      # Docker Compose для разработки
├── docker-compose.prod.yml # Docker Compose для production
├── README.md              # Общая документация
└── DEPLOY_INSTRUCTIONS.md # Эта инструкция
```

## 🔧 Настройка Production

### Переменные окружения (backend/.env)
```bash
# База данных
MONGO_URL=mongodb://mongodb:27017
MONGODB_DATABASE=numerom

# JWT
JWT_SECRET_KEY=ваш-секретный-ключ-здесь

# Frontend
FRONTEND_URL=http://localhost:5128

# Другие настройки
LOG_LEVEL=INFO
```

### SSL/HTTPS (опционально)
Для production рекомендуется настроить HTTPS. Добавьте в docker-compose.prod.yml:
```yaml
frontend:
  environment:
    - NGINX_ENFORCE_HTTPS=true
  ports:
    - "443:443"
  volumes:
    - ./ssl:/etc/nginx/ssl:ro
```

## 🛠️ Управление приложением

### Просмотр логов
```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend  
docker-compose logs -f frontend
```

### Перезапуск сервисов
```bash
# Все сервисы
docker-compose restart

# Только backend
docker-compose restart backend
```

### Остановка
```bash
docker-compose down
```

## 🔍 Диагностика проблем

### Backend не запускается
```bash
docker-compose logs backend
# Проверьте ошибки в логах
```

### Frontend не загружается
```bash
curl http://localhost:5128
# Должен вернуть HTML страницу
```

### Проблемы с базой данных
```bash
docker-compose exec mongodb mongo numerom
# Проверьте подключение к MongoDB
```

## 📞 Поддержка

Если возникнут проблемы с развертыванием:
1. Проверьте логи контейнеров
2. Убедитесь, что порты 5128, 8001, 27017 свободны
3. Проверьте настройки firewall
4. Убедитесь, что Docker и Docker Compose установлены

## 🎯 Production готовность

Проект включает:
- ✅ Оптимизированный production build frontend
- ✅ Docker контейнеры для всех сервисов
- ✅ Nginx конфигурация для frontend
- ✅ Переменные окружения для конфигурации
- ✅ Масштабируемая архитектура

---
*NumerOM - Нумерологическая платформа для самопознания*
